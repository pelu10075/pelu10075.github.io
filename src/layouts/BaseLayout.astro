---
import Header from '../components/Header.astro';
import SiteLanguage from '../components/SiteLanguage.astro';
import '../styles/global.css';
import '../styles/i18n.css';

interface Props {
	title: string;
	description?: string;
	/** 홈: 전체 너비 섹션 (패딩 없음) */
	fullWidth?: boolean;
}

const { title, description = 'Portfolio & blog', fullWidth = false } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en" data-site-lang="en">
	<head>
		<meta charset="utf-8" />
		<script is:inline>
			(function () {
				try {
					var s = localStorage.getItem('site-lang');
					if (s === 'ko' || s === 'en') {
						document.documentElement.setAttribute('data-site-lang', s);
						document.documentElement.setAttribute('lang', s === 'ko' ? 'ko' : 'en');
					}
				} catch (e) {}
			})();
		</script>
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
		<link rel="canonical" href={canonical} />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Space+Mono:wght@400;700&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<Header />
		<main class:list={['main', { 'main--full': fullWidth }]}>
			<slot />
		</main>
		<footer class="site-footer">
			<p class="i18n i18n--en">© {year} Min-Seong Park. Astro + GitHub Pages.</p>
			<p class="i18n i18n--ko">© {year} 박민성. Astro + GitHub Pages.</p>
			<p class="i18n i18n--en">Aspiring UWaterloo Engineer · FEIA Class of 2028</p>
			<p class="i18n i18n--ko">UWaterloo 공학도를 지향 · FEIA 2028 졸업 예정</p>
		</footer>
		<SiteLanguage />
	</body>
</html>

<style>
	.main {
		max-width: var(--max-wide);
		margin: 0 auto;
		padding: 5.5rem 1.5rem 4rem;
	}

	.main--full {
		max-width: none;
		margin: 0;
		padding: 0;
	}

	.site-footer {
		padding: 2rem 2.5rem;
		border-top: 1px solid var(--border);
		display: flex;
		align-items: center;
		justify-content: space-between;
		flex-wrap: wrap;
		gap: 0.5rem;
	}

	.site-footer p {
		margin: 0;
		font-family: var(--font-mono);
		font-size: 0.72rem;
		color: var(--text-dim);
		letter-spacing: 0.05em;
	}

	@media (max-width: 768px) {
		.site-footer {
			flex-direction: column;
			text-align: center;
		}
	}
</style>
